#include "main.h"

/**
 * mul - Multiplies @a by @b
 * @a: one of the values to be multiplied
 * @b: one of the values to be multiplied
 * @res:  result
 * Return:  @res
 */
int mul(int a, int b)
{
	int res = (a * b);
    return (res);
}
