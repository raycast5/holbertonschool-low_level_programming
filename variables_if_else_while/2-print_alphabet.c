#include <stdio.h>

/**
* main - prints the alphabet in lowercase letters
* Return: 0
*/

int main(void)
{
	char alfa;

	alfa = 'a';
	while (alfa <= 'z');
	putchar(alfa);
	putchar('\n');
		alfa++;

return (0);
}
