#include <stdio.h>

/**
* main - prints the alphabet in lowercase letters
* Return: 0
*/

int main(void)
{
	int alfa;

	for (alfa = 'a'; alfa <= 'z'; alfa++)
	putchar(alfa);
	putchar('\n');
return (0);
}
